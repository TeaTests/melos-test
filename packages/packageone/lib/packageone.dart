library packageone;

/// A Calculator.
class Addition {
  /// Returns [value] plus 1.
  int addOne(int value) => value + 1;

  /// Returns [first] plus [second].
  int add(int first, int second) => first + second;
}
